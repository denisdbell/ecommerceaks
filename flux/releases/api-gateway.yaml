apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: api-gateway
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: api-gateway
  chart:
    spec:
      chart: ./helm/api-gateway
      sourceRef:
        kind: GitRepository
        name: flux-system
        namespace: flux-system
      interval: 1m
  values:
    image:
      repository: ecommerceacrdenis.azurecr.io/api-gateway
      tag: 61fa634bd95d9f44e0de55dc38491bd0660376f9
    replicaCount: 2
