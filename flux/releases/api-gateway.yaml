apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: api-gateway
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: api-gateway
  chart:
    spec:
      chart: ./helm/api-gateway
      sourceRef:
        kind: GitRepository
        name: ecommerce-platform
        namespace: flux-system
      interval: 1m
  values:
    image:
      repository: ecommerceacrdenis.azurecr.io/api-gateway
      tag: 8bcdff47965a82263d24c1d16031cba21f4090e4
    replicaCount: 2
