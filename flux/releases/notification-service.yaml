apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: notification-service
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: notification-service
  chart:
    spec:
      chart: ./helm/notification-service
      sourceRef:
        kind: GitRepository
        name: ecommerce-platform
        namespace: flux-system
      interval: 1m
  values:
    image:
      repository: ecommerceacrdenis.azurecr.io/notification-service
      tag: 729a41634641b4f26e788ab30dff384e1ebc4eb3
    replicaCount: 2
