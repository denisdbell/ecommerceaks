apiVersion: helm.toolkit.fluxcd.io/v2
kind: HelmRelease
metadata:
  name: notification-service
  namespace: flux-system
spec:
  interval: 5m
  targetNamespace: notification-service
  chart:
    spec:
      chart: ./helm/notification-service
      sourceRef:
        kind: GitRepository
        name: ecommerce-platform
        namespace: flux-system
      interval: 1m
  values:
    image:
      repository: ecommerceacrdenis.azurecr.io/notification-service
      tag: c14d329548dd8fa0143c0c0b7a2baaa18ecba03a
    replicaCount: 2
